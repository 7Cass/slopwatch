# User-local global configuration

Slopwatch v0 uses user-local global configuration, such as `~/.config/slopwatch/config.json`, rather than repository-local configuration. The product observes Codex activity across many projects for one local user, so source overrides and local defaults belong to the user's Slopwatch environment. Project-specific configuration can be added later if repeated use shows real pressure for it.
